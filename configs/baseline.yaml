# M2 naive one-step baseline configuration.
seed: 0
device: auto   # auto -> cuda if available, else cpu

# Target pi_1 (must match configs/teacher.yaml geometry).
data:
  side: 1.0
  gap: 0.0

teacher_checkpoint: outputs/m1/teacher.pt

eval:
  n_samples: 2000

eval_noise: outputs/eval_noise.pt   # fixed x_T shared with M3/M4
output_dir: outputs/m2
