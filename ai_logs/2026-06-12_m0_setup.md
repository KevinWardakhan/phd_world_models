# 2026-06-12 - M0 repository + data setup

## Goal
Stand up a clean, one-command-runnable repository and complete milestone M0
(2D source/target data, plots, sanity metrics). No teacher/DMD/DMD2 yet.

## Tools
- ChatGPT: refined the roadmap and the Codex prompt for this step (see [chatgpt_planning.md](chatgpt_planning.md)).
- Codex: implemented the repo skeleton, `src/` data + metrics + plotting code, resources index, AI logs, and README.

## What was done
- Repo layout under `src/`, `configs/`, `outputs/`, `resources/`, `ai_logs/`.
- `pi_0` isotropic Gaussian and `pi_1` 8-mode checkerboard (3x3 grid, center removed).
- M0 metrics: recovered mode count, normalized mode entropy, off-support fraction, pi_0 moments; energy-distance/MMD verified via a same-distribution self-test.
- Single entry point `./run.sh m0` (bootstraps a venv, installs deps, runs M0).