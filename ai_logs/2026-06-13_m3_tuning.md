# 2026-06-13 - M3.1 temporary hyperparameter tuning

## Goal
Find slightly better M3 DMD hyperparameters before M4, without redesigning M3.
M3 already works (all variants recover 8/8 modes and beat the M2 one-step
teacher). The tuning code is temporary and must be easy to delete; it must not
make the clean M3 pipeline depend on tuning infrastructure.

## Tools
- ChatGPT: refined the tuning plan, search space, and ranking metric (see [chatgpt_planning.md](chatgpt_planning.md)).
- Codex: implemented the isolated tuning config + script + docs (no run; user runs on GPU).

## Files touched (all removable)
- New: `configs/dmd_tuning.yaml`, `scripts/tune_m3_hparams.py`, `scripts/__init__.py`, `outputs/m3_tuning/` (created at run time).
- Docs: `README.md` (new "M3.1" subsection), `ai_logs/index.md` row, this entry.
- Not modified: any `src/` M3 code, `configs/dmd.yaml`, `run.sh`.

## Temporary tuning design
- Dependency direction is tuning -> M3 only: the script imports and reuses
  `src.train_dmd.train_variant` / `resolve_device`, `src.dmd`, `src.metrics`,
  `src.plotting`, `src.distributions`, `src.diffusion.load_teacher`,
  `src.utils`. Nothing in `src/` imports the tuning code.
- Not wired into `run.sh`; invoked as
  `python -m scripts.tune_m3_hparams --config configs/dmd_tuning.yaml`.
- Reuses the shared 2000-sample `outputs/eval_noise.pt` for plots and a separate
  fixed 10000-sample noise (`outputs/m3_tuning/eval_noise_10k.pt`) for metrics;
  the shared file is left untouched. Each trial reseeds to the same noise stream
  for a fair comparison.

## Search space (~15 curated trials, one factor at a time around `dmd_no_reg`)
- `lambda_reg` in {0.0, 0.025, 0.05, 0.10, 0.25}; `reg_loss_type` in {mse, smooth_l1, l1} (only if `lambda_reg>0`).
- `fake_updates_per_gen` (TTUR) in {1, 2, 3, 5}.
- `gen_lr` in {5e-5, 1e-4, 2e-4}; `fake_lr` in {5e-4, 1e-3, 2e-3}.
- one combo: `lambda_reg=0.05` (mse) + `fake_updates_per_gen=3`.
- `t_gen` fixed at `T-1`; DMD noise-time range fixed at the M3 baseline (0.02-0.98).

## Ranking metric
- Hard gate (mode coverage primary): reject `recovered_mode_count < 8` or `normalized_mode_entropy < 0.98` (penalized, still reported).
- Composite among valid trials: `score = energy_distance + mmd_rbf + 0.05 * off_support_fraction`.
- Justification: the brief's 0.25 off-support weight would dominate ED+MMD at M3
  magnitudes (0.045 vs ~0.008), inverting the intended priority; 0.05 keeps it a
  true tie-breaker. Both 0.05 and 0.25 composites plus raw metrics are reported.

## Best result (after the GPU run, n=5000 ranking)
- Best trial: `ttur3` = no regression, `fake_updates_per_gen=3`, `gen_lr=1e-4`,
  `fake_lr=1e-3`. Metrics: 8/8 modes, entropy 0.999, off-support 0.141,
  ED 0.0016, MMD 0.0005, composite 0.0092.
- Improves over the original M3 best (`baseline_no_reg`, composite 0.0220): yes, ~2.4x lower.
- Regression helped: yes (all reg variants beat the naive baseline; `smooth_l1`/0.05 best reg-only).
- TTUR helped: yes, the most (fu=2/3/5 all beat fu=1; fu=3 best overall).
- Key correction: `baseline_no_reg` (fu=1), which looked best in the original tiny
  3-variant ablation, is actually the WORST valid trial (rank 15). The earlier
  "regression/TTUR not needed" conclusion was an artifact of too small a search;
  the DMD/DMD2 message holds.

## Outcome / cleanup
- `configs/dmd.yaml` updated to the selected settings: `dmd_no_reg_ttur` uses
  `fake_updates_per_gen=3` (was 5); `dmd_reg` uses `smooth_l1`/0.05 (was mse/0.25);
  learning rates unchanged. The naive `dmd_no_reg` (fu=1) is kept as the weak
  reference arm.
- Temporary tuning code/outputs removed per request: `configs/dmd_tuning.yaml`,
  `scripts/` (`tune_m3_hparams.py`, `__init__.py`), `outputs/m3_tuning/`. Only the
  documented results (README "M3.1", this log) are kept.
- User reruns `./run.sh m3` to regenerate `outputs/m3/` with the tuned config.

## What was manually checked
- Syntax compile of the script passed (`py_compile`) before the GPU run.
- Reviewed `trials_summary.md`, `best_metrics.json`, `best_config.yaml`, and the
  `best_variant_*.png` figures (8/8 modes, balanced) before deleting the outputs.

## Raw transcript
Place the raw Codex/ChatGPT export for this session in `transcripts/`. Not invented here.
